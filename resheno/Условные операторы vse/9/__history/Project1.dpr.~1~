program Project1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

Var mr, yr, m, y, mm, yy: integer;

begin
  write(' мес€ц и год рождени€: ');
  readln(m, y);
  write(' мес€ц и год расчЄта: ');
  readln(mr, yr);
  yf := y - yr;
  mf := m - mr;
  if mf < 1 then
  begin
    yy := yy - 1;
    mm := mm + 12 ;
  end;
  writeln('лет - ', yy, ', мес€цев - ', mm);
readln;
end.
